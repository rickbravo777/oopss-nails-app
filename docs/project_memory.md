# Project Memory

## Current State

- **Project**: Oopss Nails — Asistente Virtual de Agendamiento
- **Phase**: **Delivered, mid-deployment.** M0/{M1+M2}/{M3+M4} all reviewed (PASS). Final holistic pre-delivery security audit complete. All 19 User Journeys + 10 Infrastructure Tasks built, verified live, reviewed, security-audited. `git init` done (iteration #16), repo pushed to GitHub. EasyPanel deployment started (iteration #17) but **paused mid-setup, not live yet** — see the 🔴 EXACT RESUME POINT below. Docker still not live-built anywhere — first real build will happen on the EasyPanel host itself.
- **✅ RESOLVED 2026-08-08 (iteration #18)**: the long-running "admin no funciona"/"se queda cargando" saga (iterations #14–#17) is closed. Root cause: the admin JWT expires after 24h (normal/expected), the backend correctly rejected the stale token with a 401, but the frontend had no handling for a 401 at all — every admin page just showed a generic error instead of prompting re-login. Fixed in `apiClient.ts`: a 401 on an authenticated call now clears the stale token and redirects to `/admin/login`. If the client reports "admin no funciona" again, first ask whether she just needed to log back in (expected) before assuming it's a new bug.
- **🔴 EXACT RESUME POINT — EasyPanel deployment (iteration #17, still not advanced further)**: client was about to paste the environment variables into `oopss-app` → "Entorno" (EasyPanel project `criteriabravo`) and save — session got pulled into local bug-fixing (iterations #18–#21) before returning to this. Still needed, in order: (1) confirm env vars saved, (2) attach a persistent volume at `/app/uploads` on `oopss-app` (**not done** — skip this and uploaded client photos vanish on every redeploy), (3) click "Implementar" for `oopss-app`'s very first build ever — **this Dockerfile has never actually been built by anyone**, watch the logs closely, (4) set up a domain + update `CORS_ALLOWED_ORIGIN` to match, (5) log into `/admin` with her chosen `ADMIN_INITIAL_EMAIL`/`PASSWORD` and paste the real `OPENAI_API_KEY` into `/admin/credenciales`. Production secret values (`JWT_SECRET` etc.) were generated in iteration #16 and given to her in iteration #17 — don't regenerate, they should already be sitting in what she's about to paste. Repo is now **public** on GitHub (client's choice, to work around an EasyPanel GitHub App permission gap — no secrets in history, reversible later if she wants it private again).
- **Last Completed**: Post-delivery iteration #26 — client reported "que servicios ofrecen?" sometimes producing picker-inviting text ("...elige el que prefieras") with zero buttons underneath. Could not reproduce after 7 live attempts (real API + real browser with a fresh session and a live JS error listener) — concluded it's an intermittent LLM sampling miss, same class as iteration #8's original picker-reliability problem. Rather than chase an unreproducible prompt tweak, strengthened `withServiceSelectionFallback()` with a 4th deterministic trigger: if the reply uses the app's own standard "elige el/la que prefieras" phrasing without the tool actually being called, the fallback now fires. Also separately confirmed (via the same live-listener technique) that an earlier "need to refresh" report was leftover Vite Fast Refresh churn from active code editing, not a real bug — **note for future sessions: this browser tool's console-message history appears to persist across `navigate` calls, so install a fresh `console.error` listener immediately before the action under test rather than trusting `read_console_messages` at face value.** Backend suite 208→211.
- **Last Completed (previous)**: Post-delivery iteration #25 — iteration #24's fix (correct-name-before-booking) still failed intermittently on live re-test; traced to the real architectural gap: conversation history rebuilt for each turn only carries role+content, never the actual tool-call/tool-result exchange — so if the model's earlier prose ever drifted from what `get_service_info` actually returned (a second, independent failure point iteration #24 didn't cover), a later "sí, agendemos" turn had nothing accurate to work from. Fixed deterministically, not with more prompt wording: new `backend/src/lib/ai/confirmedServices.ts` re-derives real validated service names from each turn's already-stored `toolCallMeta` and injects them fresh as a system message on every call (same ground-truth-injection principle as DEC-14's date handling). Backend suite 201→208. Verified live: the exact previously-flaky sequence now resolves correctly 5/5.
- **Last Completed (previous)**: Post-delivery iteration #24 — a screenshot showed "me interesaria agendar mani spa" produced the generic 10-category picker plus prose asking for booking details, instead of the specific service: the model had guessed a paraphrased name ("Manicure Spa" instead of the real "Mani Spa (regular)") straight into `offer_service_selection` without calling `get_service_info` first. Fixed via prompt: when a client names a service AND asks to book it in one message, the model must call `get_service_info` first to resolve the real name, then `offer_service_selection` with that validated name. Also added a requested confirm-first safety net: `offer_service_selection` gained `confirmFirst: boolean` — when set, the frontend shows a single-tap "¿Quieres agendar X? Sí/No" before proceeding, instead of assuming an uncertain spelling-match is correct. Backend suite 199→201. Verified live 3/3 across two services that the real-name-first sequence now works correctly.
- **Last Completed (previous)**: Post-delivery iteration #23 — after the assistant clarifies a service via Q&A (`get_service_info`) and the client confirms she wants to book it ("si creemos cita"), it now jumps straight to the visual especialista→fecha→hora→datos selector instead of asking for those details in text. `offer_service_selection` gained an optional `serviceName` param (validated against the real active-service table, mirrors the existing `categories` param); `ServiceBookingFlow.tsx` gained `initialServiceId`, auto-selecting that service once the catalog loads and skipping categoría/servicio entirely. Backend suite 197→199. Verified live reproducing the client's exact scenario (Relleno Polygel Q&A → confirm → visual especialista picker with zero prose asking for booking details).
- **Last Completed (previous)**: Post-delivery iteration #22 — found during a pre-client-meeting smoke test: `get_service_info` couldn't find "Extensión Polygel" from the client's real phrasing "extension en polygel" (missing accent + an inserted word broke the old single-substring match). Rewrote the tool's matching to split the query into significant words (accent/case-normalized, common Spanish stopwords filtered) and require every word to appear in the service's name+category, in any order — handles both missing accents and natural word insertion without over-matching (verified a shared-category-word case still narrows correctly). Backend suite 193→197. Verified live: the exact reproducing question now answers correctly, plus a second accent-dropped test.
- **Last Completed (previous)**: Post-delivery iteration #21 — added a real "Reagendar" flow to the admin appointment-edit modal that can change **specialist**, not just date/time (a genuine backend gap — `rescheduleAppointment()` previously only ever re-validated against the appointment's existing specialist; no code path anywhere could move an appointment to different staff). `rescheduleAppointment()` gained an optional `newSpecialistId` param, re-validated via the same `checkAvailability()` engine as every other booking path (also fixed a latent bug: `endTime` now comes from the validated slot instead of a stale duration snapshot, which mattered once a *different* specialist's own duration override could apply). New single-tap especialista→hora sub-flow in `AppointmentsAdminPage.tsx`. Backend suite 191→193. Verified end-to-end with a real disposable appointment (Tania→Mariangely + new time, confirmed in Postgres, cleaned up).
- **Last Completed (previous)**: Post-delivery iteration #20 — the admin appointment-edit modal had two real bugs: (1) the "Estado" dropdown's unselected options were unreadable (no explicit background, browser defaulted the popup to white) — found and fixed the identical pattern in 8 other `<select>` elements across 4 admin pages; (2) "Cancelar" was actually just the dialog's dismiss button, not a real cancel action — clicking it "did nothing" from the client's perspective. Added a real "Cancelar cita" destructive button with inline confirm (mirrors `/mis-citas`'s pattern), renamed the dismiss button to "Cerrar". Verified end-to-end against a real disposable test appointment.
- **Last Completed (previous)**: Post-delivery iteration #19 — five agent knowledge/UX fixes found via the client's own live testing: (1) added a missing "Peinado" (hairstyling) service (Aurora, $25 desde); (2) `offer_service_selection` gained an optional `categories` param (server-validated) so "las uñas" narrows the picker to a filtered Manos/Pies pair instead of showing all 10 categories — iterated live with the client to get the exact UX right (filtered single-tap step, not a merged list); (3) caught and fixed a service-name-hallucination regression by adding `temperature: 0.2` to both OpenAI calls (a stronger prompt instruction alone wasn't reliably enough). Backend suite 188→191.
- **Post-delivery iterations #1–#14, #16 status: all DONE** (compact index — full detail in `docs/work_log.md`'s dated entries): brand palette+calendar (#1), guided booking modal (#2), chat welcome (#3), agent brand polish — greeting+logo (#4), phone-format/tool-loop fixes (#5), single-tap selection everywhere incl. inline AI picker (#6), specialist name-variant + date-hallucination fixes (#7), deterministic first-reply picker safety net (#8), copy polish (#9), escalation over-eagerness fix (#10), SSE reconnect fixes (#11), running-history booking steps redesign (#12), Google Sheets sync + email field + input formatting — **code done, not yet activated, see Blockers #8** (#13), real root cause of the chat "necesito refrescar" bug — dropped SSE reply (#14), `git init` + Docker/EasyPanel review + secrets generated (#16). Don't re-expand these back into full paragraphs here — this file is meant to stay a compact pointer, not a duplicate of `work_log.md`.
- **Next Step**: (1) **Deployment — see the 🔴 EXACT RESUME POINT above.** This is the main open thread; everything else this session was a detour into bug fixes found while testing locally. (2) **Blocked on the client**: Google Sheets sync (iteration #13) needs her Google Cloud service account + spreadsheet ID pasted into `/admin/credenciales`. If resumed in a future session, start with `/session-start` — read this file, then `implementation/task_tracker.md`.
- **Dev-server stability note**: local dev servers (via `preview_start`) don't persist between sessions — if the client reports "no me deja verlo" at the start of a new session, first check whether `backend`/`frontend` are actually running (`curl http://localhost:3000/api/v1/services`, `curl http://localhost:5173`) before troubleshooting further.
- **Known Vite-dev-proxy quirk (iteration #11)**: if the backend restarts (e.g. `tsx watch` reload after a code edit) while a browser tab has an open chat, Vite's dev proxy can get stuck on the `/api/.../stream` (SSE) route even though the backend is confirmed healthy directly on :3000 — one browser refresh clears it. Dev-only, not present in the deployed app.

## Blockers

1. UJ-05 (photo attach in chat) still needs a live file-picker test — backend proven (IT-07), frontend wiring built, but browser-tool automation can't simulate a real file picker dialog.
2. IT-08's Docker config still not live-built anywhere — first real build will be on the EasyPanel host, per the client's explicit choice (see 🔴 EXACT RESUME POINT).
3. ~~No git repository initialized.~~ **Resolved 2026-08-07 (iteration #16).**
4. (nice-to-have, deferred) `POST /chat/sessions/:id/escalate` is documented in `design/api_contracts.md` but doesn't exist as a route — escalation only happens via the AI tool. Needs a decision-log entry or a doc update.
5. (known, accepted — DEC-10) `EscalationFlag.resolutionNotes` is a single field reused for both the AI's original escalation-context note and the admin's eventual resolution note. Low impact, not planned to be fixed unless it matters in practice.
6. (known, accepted — DEC-11) `AssistantPolicy.value` (UJ-18 policy text) has no prompt-injection content sanitization beyond a length cap — accepted given the single-admin trust model.
7. ~~Logo integration.~~ Resolved 2026-08-06 — favicon, chat header, admin login, admin sidebar all use the real logo.
8. **(active) Google Sheets sync built but not activated.** `backend/src/lib/googleSheets.ts` is done, tested, wired into every booking path — silently no-ops until the client provides real Google Cloud credentials (service account JSON + spreadsheet ID, pasted into `/admin/credenciales`). No code changes needed once she provides these.
9. ~~Admin "Citas" / general admin stuck-on-"Cargando…".~~ **Resolved 2026-08-08 (iteration #18)** — root cause was an expired 24h JWT with no client-side 401 handling.
10. **(active) Deployment to EasyPanel in progress, not live yet.** See the 🔴 EXACT RESUME POINT near the top of this file.
11. **(active) GitHub repo `rickbravo777/oopss-nails-app` is public.** Client's choice (worked around an EasyPanel GitHub App permission gap). No secrets in history. Reversible to private + scoped GitHub App access later if it matters to her (~5 min fix).

## Recent Changes

Full dated history lives in `docs/work_log.md` — every iteration has its own entry there with Work Done / Files / Decisions / Security Check / Tests / Notes. This section is intentionally not duplicated here; see the "Last Completed" chain above for the most recent few, and `work_log.md` for anything older.

## Key Context for Next Session

- Stack: Node.js+Express+TypeScript backend, React+TypeScript+Vite+Tailwind frontend, PostgreSQL 17 (locally installed as a Windows service) + Prisma, JWT+bcrypt admin auth, OpenAI SDK with tool-calling.
- **Local dev database**: PostgreSQL 17 running as Windows service `postgresql-x64-17`. Superuser `postgres`/`postgres`. App role/db: `oopss` / `oopss_dev_pw` / `oopss_nails`. Real values in `backend/.env` (gitignored) — regenerate from `backend/.env.example` if missing. `npm run seed` populates real reference data (catalog/staff/schedules/term synonyms/admin user) **plus intentional demo data** (5 fake clients on the `809555100X` phone block, demo appointments/conversations/escalations) — idempotent, safe to re-run.
- `.claude/launch.json` has `frontend` (5173) and `backend` (3000) dev configs for `preview_start`. **Restart the backend after any `backend/.env` or `schema.prisma` change** — `tsx watch` doesn't reload env vars, and a running process can lock the Prisma query engine DLL on Windows (kill it before `prisma generate`).
- Seeded admin (dev only): `admin@oopssnails.local` / `DevOnly_ChangeMe123`. `OPENAI_API_KEY` is configured in BOTH `backend/.env` AND the Level-3 vault (vault takes priority).
- **House rules established**: (1) all user-facing strings must be Spanish, verified by looking at rendered output; (2) `prisma migrate dev` doesn't work non-interactively here — hand-write migration SQL + `prisma migrate deploy`; (3) a git repo exists as of 2026-08-07 (iteration #16), local-only `user.name`/`user.email` set on it; (4) talk to the user in Spanish in chat, code/comments stay in English; (5) for any endpoint returning the same kind of resource from multiple routes, make sure the response shape is identical everywhere; (6) for AI-behavior-dependent journeys, always test actual live model output, not just the plumbing; (7) when multiple rows share an identical action-button label, scope any DOM query used for live verification to that specific row's own container; (8) **a persistent prompt-violation that resists a stronger instruction is worth checking sampling settings (`temperature`) for**, not just rewording the prompt again and again (iteration #19).
- Browser-tool note: clicking by a cached `ref` from an earlier `read_page` can silently fail to trigger React handlers. Workaround: `element.click()` / `form.requestSubmit()` via `javascript_tool`, or re-`read_page` immediately before clicking.
- Read `design/design_summary.md` first every session. Check `implementation/task_tracker.md` for authoritative per-item status.
- Milestone order: M0 → M1+M2 → M3 → M4 → {M3+M4} review → demo data/empty-state verification → final security audit → **delivered** → post-delivery iterations #1–#21 (see the compact index above and `implementation/task_tracker.md`'s Post-Delivery Iterations table for per-iteration status) → **currently mid-EasyPanel-deployment, paused at the 🔴 EXACT RESUME POINT above.**
- Open assumptions (see `planning/questions.md`): currency=USD, Acrylic extension = Tania/Mariangely, no specialist logins in v1, estimated durations, no client password accounts.
- `/review` ran after M0, after {M1+M2}, and after {M3+M4} — all closed. The mandatory pre-delivery holistic security audit also ran and closed 2026-08-04 — see `docs/work_log.md`'s "Final holistic pre-delivery security audit" and its "Residual Risks" section.
