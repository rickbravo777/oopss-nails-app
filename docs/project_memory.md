# Project Memory

## Current State

- **Project**: Oopss Nails — Asistente Virtual de Agendamiento
- **Phase**: **Delivered, now moving into deployment.** M0 reviewed (PASS). {M1+M2} reviewed (PASS, post-review fix applied). {M3+M4} reviewed (PASS, post-review fix applied). Final holistic pre-delivery security audit complete (PASS, 2 should-fix findings fixed same-session). Demo-data seed pass, full-page live verification, and empty-state verification all done. All 19 User Journeys + 10 Infrastructure Tasks are built, verified live, independently reviewed, and security-audited end to end. **`git init` is now done** (iteration #16, first commit made, verified clean of secrets). Docker still not live-built locally (no Docker on this dev machine) — plan is to build it for the first time directly on the real EasyPanel host, per the client's explicit choice.
- **✅ RESOLVED 2026-08-08 (iteration #18)**: the long-running "admin no funciona"/"se queda cargando" saga (iterations #14–#17) is now believed genuinely closed. Real root cause: the admin JWT expires after 24h (`JWT_EXPIRATION=24h`, normal/expected), the backend correctly rejected the stale token with a 401, but `frontend/src/lib/apiClient.ts` had **no handling for a 401 at all** — every admin page's `isError` state (iteration #15) rendered a generic "No se pudo cargar…" instead of anything indicating "your session expired, log in again." Fixed: `apiFetch()` now detects a 401 on an authenticated call, clears the stale token, and redirects to `/admin/login`. Verified live by corrupting the stored token and confirming the redirect fires instead of the stuck-error state. This most likely explains *every* prior "stuck loading" report across #14–#17, not just today's — those sessions all had real-time gaps that could have silently crossed the 24h boundary. If the client reports this again, first ask whether she just needed to log back in (expected, not a bug) before assuming it's a new issue.
- **Last Completed**: Post-delivery iteration #21 — added a real "Reagendar" flow to the admin appointment-edit modal that can change **specialist**, not just date/time — this was a genuine backend gap (`rescheduleAppointment()` previously only ever re-validated against the appointment's existing specialist; there was no code path anywhere, chat/guided/admin, that could move an appointment to different staff). `rescheduleAppointment()` gained an optional `newSpecialistId` param (re-validates via the same `checkAvailability()` engine as every other booking path — also fixed a latent bug where `endTime` was recomputed from a stale duration snapshot instead of the validated slot's own `endTime`, which mattered once a *different* specialist's own duration override could apply). New single-tap especialista→hora sub-flow in `AppointmentsAdminPage.tsx`, reusing the public `/availability/check` endpoint with `specialistId` omitted to see every qualifying specialist. Backend suite 191→193. Verified end-to-end with a real disposable appointment (Tania→Mariangely + new time, confirmed in Postgres, cleaned up).
- **Last Completed (previous)**: Post-delivery iteration #20 — the admin appointment-edit modal (opened by clicking any appointment in Citas) had two real bugs: (1) the "Estado" `<select>` dropdown's unselected options were unreadable (light-on-white, since no explicit background was set and browsers default the native dropdown popup to white) — found the identical `bg-transparent`-with-no-background pattern in 8 other selects across 4 admin pages and fixed all of them; (2) the "Cancelar" button was actually just the dialog's dismiss button, not an appointment-cancel action — clicking it "did nothing" from the client's perspective. Added a real "Cancelar cita" destructive button with an inline confirm step (mirrors `/mis-citas`'s existing client-facing cancel pattern), renamed the dismiss button to "Cerrar". Verified end-to-end against a real disposable test appointment (created via the public API, cancelled via the new UI button, confirmed `status: cancelled` directly in Postgres, then cleaned up).
- **Last Completed (previous)**: Post-delivery iteration #19 — five agent knowledge/UX fixes found via the client's own live testing, all verified live: (1) added a missing "Peinado" (hairstyling) service to the catalog (Aurora, $25 desde); (2→4, iterated with the client watching it in practice) `offer_service_selection` now accepts an optional `categories` param (server-validated against the real `ServiceCategory` table) — a single implied category skips straight to its services, but two or more (e.g. "uñas" → Manos+Pies) now correctly show as a **filtered single-tap category step** ("Manos"/"Pies" as two pills), not a merged combined list as the first draft did; (3→5) caught a service-name-hallucination regression twice while testing live, fixed for good by adding `temperature: 0.2` to both OpenAI calls in `openaiProvider.ts` (a stronger prompt instruction alone wasn't reliably enough) — 3/3 clean on re-test afterward. Also caught and fixed a self-inflicted syntax bug mid-session (stray backticks broke the `BASE_PROMPT` template literal, failing 15/25 test files) before calling it done. Backend suite 188→191. See `docs/work_log.md`'s "iteration #19" entry for full detail.
- **Last Completed (previous)**: Post-delivery iteration #18 — found and fixed the true root cause of the recurring "admin no funciona" complaint: an expired 24h JWT with no client-side 401 handling, so every page silently failed instead of prompting re-login. See the ✅ RESOLVED note above and `docs/work_log.md`'s "iteration #18" entry for full detail.
- **Last Completed (previous)**: Post-delivery iteration #17 — EasyPanel deployment in progress, **paused mid-setup, not live yet**. Pushed the repo to `https://github.com/rickbravo777/oopss-nails-app` (now **public** — made public to work around an EasyPanel GitHub App permission error; no secrets in history, but source code is now publicly visible — client's explicit choice, reversible later, see Blockers #10). Created `oopss-postgres` and `oopss-app` services inside the client's existing `criteriabravo` EasyPanel project (no free project slot on her license — co-location is purely organizational, not an architecture change). Confirmed internal Postgres host for `DATABASE_URL`: `criteriabravo_oopss-postgres:5432`. Selected "Dockerfile" as `oopss-app`'s build method. Gave the client the full env-var block with the real `DATABASE_URL` assembled — **she had not yet pasted/saved it when the session paused.**
- **🔴 EXACT RESUME POINT (read this before doing anything else)**: client was about to paste the environment variables into `oopss-app` → "Entorno" and save. Still needed after that, in order: (1) confirm env vars saved, (2) attach a persistent volume at `/app/uploads` on `oopss-app` (**not done yet** — skip this and uploaded client photos vanish on every redeploy), (3) click "Implementar" for `oopss-app`'s very first build ever — **this Dockerfile has never actually been built by anyone**, watch the logs, be ready for a real build failure, not just a rubber-stamp confirmation, (4) set up a domain + update `CORS_ALLOWED_ORIGIN` to match, (5) log into `/admin` with her chosen `ADMIN_INITIAL_EMAIL`/`PASSWORD` and paste the real `OPENAI_API_KEY` into `/admin/credenciales`. The production secret values (`JWT_SECRET` etc.) are the same ones generated in iteration #16 and repeated to her in iteration #17 — don't regenerate them, they should already be sitting in what she's about to paste.
- **Last Completed (previous)**: Post-delivery iteration #16 — the client hit "stuck on Cargando…" again on the admin "Citas" page, this time a genuinely different bug from #14: `AppointmentsAdminPage.tsx`'s React Query calls (`weekQuery`/`dayQuery`/`listQuery`/`specialistsQuery`) checked `.isLoading` but never `.isError`, so a failed fetch (retries exhausted) left the page frozen on "Cargando…" forever with no error and no way to retry short of a full reload — `DashboardPage.tsx` already had the correct `isLoading`/`isError` pair, this page just never got it. Added the same pattern (error message + "Reintentar" button) to all four queries there and to `ServicesAdminPage.tsx`'s `servicesQuery`. Audited the other admin pages (Personal/Credenciales/Base de Conocimiento/horarios) — none render a "Cargando…" string at all, so none share this exact symptom. No backend changes; build/lint clean; verified live that both pages render real data correctly. Could not force a clean synthetic-failure repro in-session (dev environment's own Vite HMR/WS churn got in the way), so this one is verified by code review + consistency with the already-proven Dashboard pattern, not a forced-failure screenshot.
- **Last Completed (previous)**: Post-delivery iteration #14 — found and fixed the REAL cause of "necesito refrescar para ver la respuesta" (iteration #11 had fixed two real but insufficient reconnect bugs; the client kept hitting the symptom afterward). First tried making the chat's SSE stream connect directly to the backend (bypassing Vite's dev proxy) — this made it WORSE (reproduced 2-for-2 on a clean session with no restarts involved) and was reverted immediately. Root cause, found via direct DB inspection: the reply was *always* being generated and saved correctly server-side — the bug was in `ChatPage.tsx`'s `"done"` SSE handler, which tried to `.map()` a streaming placeholder message that only gets created inside the `"delta"` handler; if a reply arrives in too few chunks for any `"delta"` to land before `"done"` fires, there's no placeholder to update, and `.map()` silently matches nothing — the finished reply is discarded even though `sending` correctly flips back to `false`. Fixed by appending the message directly when no placeholder exists. Also added a polling safety net (`schedulePollFallback`, generation-counter guarded) as a second layer of protection for genuine disconnects. Verified live: 3 consecutive messages in one conversation, 3-for-3 rendered live with zero manual refresh (up from failing nearly every time just before this fix, in the same test conditions). No backend changes; 188/188 unaffected. See `docs/work_log.md`'s "iteration #14" entry for the full investigation trail, including the reverted dead-end.
- **Last Completed (previous)**: Post-delivery iteration #13 — Google Sheets live sync (built, not yet activated — needs the client's real Google Cloud credentials) + an email field added to the booking contact step + input placeholders/formatting for name/phone/email. New `backend/src/lib/googleSheets.ts` (`appendAppointmentToSheet()`, fire-and-forget, fail-silent) reads two new Level-3 credentials (`GOOGLE_SERVICE_ACCOUNT_JSON`, `GOOGLE_SHEETS_SPREADSHEET_ID`, same vault pattern as `OPENAI_API_KEY`) and appends one row per booking; wired into `booking.ts`'s `createAppointment()` so it covers chat/guided-booking/admin uniformly. `CredentialsAdminPage.tsx` got a textarea for the multi-line JSON key + help text. Added `clientEmail` to `useBookingFlow.ts`/`BookingStepsPanel.tsx` (previously missing from the click-only flow entirely) with live validation, plus extracted the phone formatter (`formatPhoneInput`) into a shared `frontend/src/lib/phoneFormat.ts` used by both the contact step and `/mis-citas`. Backend suite 182→188. Verified live end-to-end (booked with the new email field, confirmed `Client.email` persisted, confirmed the Sheets sync silently no-ops with zero ErrorLog rows since no real credentials are set yet). **Blocked on the client**: needs to create a Google Cloud service account + share a spreadsheet with it, then paste both values into `/admin/credenciales` — instructions given directly in chat. See `docs/work_log.md`'s "iteration #13" entry.
- **Post-delivery iterations #1–#12 status: all DONE.** Brand palette+calendar, guided booking modal, chat welcome, agent brand polish (greeting+logo), phone-format/tool-loop fixes, single-tap selection everywhere (incl. inline AI picker), specialist name-variant + date-hallucination fixes, picker reliability hardening (x2), copy polish, escalation over-eagerness fix, SSE reconnect fixes, and the running-history redesign of the booking steps — full detail per iteration is in `docs/work_log.md` (dated entries) and the Recent Changes list below. Kept here only as a compact index so this file doesn't grow unbounded; don't re-expand old iterations back into this block.
- **Next Step**: (1) **Deployment, in progress — see the 🔴 EXACT RESUME POINT near the top of this file.** Short version: confirm env vars saved on `oopss-app` → attach `/app/uploads` volume → first "Implementar" (watch build logs closely, unverified build) → domain + `CORS_ALLOWED_ORIGIN` → real `OPENAI_API_KEY` into `/admin/credenciales`. (2) **Re-check the "Citas" stuck-on-Cargando complaint** — see the OPEN/UNRESOLVED note above; do not report it as fixed until independently confirmed by the client, ideally once the app is live and off local dev. (3) **Blocked on the client**: Google Sheets sync (iteration #13) needs her to create a Google Cloud service account + share a spreadsheet with it, then paste `GOOGLE_SERVICE_ACCOUNT_JSON` + `GOOGLE_SHEETS_SPREADSHEET_ID` into `/admin/credenciales` — the code is done and waiting. If resumed in a future session, start with `/session-start` — read this file, then `implementation/task_tracker.md`.
- **Dev-server stability note for this session**: the frontend/backend dev servers spun up via the Browser-pane preview tool keep dying between turns (observed repeatedly). When the user needs to view the app in their own regular browser (not just the Claude Browser pane), start both with plain `npm run dev` in `backend/` and `frontend/` as persistent background processes — that's what's currently keeping the app viewable outside this session. If the user reports "no me deja verlo" again, check `curl http://localhost:3000/api/v1/ping` and `curl http://localhost:5173` first before troubleshooting further; a stray orphaned `node.exe` from an earlier session can also hold a port after the tracked preview process is gone (`Get-NetTCPConnection -LocalPort <port> -State Listen` → `Stop-Process` on the PID it reports).
- **Known Vite-dev-proxy quirk (iteration #11)**: if the backend is restarted (e.g. from a `tsx watch` reload after a code edit) while the user's browser tab has an open chat, Vite's dev proxy can get stuck specifically routing the `/api/.../stream` (SSE) route, even though the backend itself is confirmed healthy and reachable directly on :3000 — a single browser refresh clears it. This is a dev-proxy-only limitation (confirmed via curl comparisons), not present in the deployed app. If the user reports needing to refresh after *I've* just restarted the backend mid-session, this is why — tell them to refresh once, don't chase it as a new bug.

## Blockers

1. UJ-05 (photo attach in chat) still needs a live file-picker test — backend proven (IT-07), frontend wiring built, but this session's browser tool can't simulate a real file picker dialog.
2. IT-08's Docker config still not live-built — Docker not installed here; **client explicitly chose (2026-08-07) to test the build for the first time directly on the real EasyPanel host** rather than install Docker locally first. Verify the build succeeds as the very first step of the actual deploy.
3. ~~No git repository initialized yet.~~ **Resolved 2026-08-07 (iteration #16)**: `git init` done, first commit made, verified clean of secrets/`node_modules`/uploads.
9. ~~Admin "Citas" page / general admin stuck-on-"Cargando…".~~ **Resolved 2026-08-08 (iteration #18)**: root cause was an expired 24h JWT with no client-side 401 handling — see the ✅ RESOLVED note near the top of this file.
10. **(active, 2026-08-08) Deployment to EasyPanel in progress, not live yet.** GitHub repo pushed, `oopss-postgres` + `oopss-app` services created inside her `criteriabravo` project, `DATABASE_URL` assembled and given to her along with the rest of the env-var block — **not yet confirmed saved**. See the 🔴 EXACT RESUME POINT near the top of this file for the precise next steps (volume, first build, domain, `OPENAI_API_KEY`). No successful build of this `Dockerfile` has happened anywhere yet — treat the first "Implementar" as genuinely unverified, not a formality.
11. **(active, 2026-08-08) GitHub repo `rickbravo777/oopss-nails-app` is public.** Made public (client's choice) to work around EasyPanel's GitHub App not having access to the private repo. Confirmed no secrets in tracked history, so no direct leak — but source code (business logic, system prompts, pricing) is now visible to anyone with the URL. If this matters to the client, switch back to private + grant the EasyPanel GitHub App scoped access instead (~5 min fix, not done yet).
4. (nice-to-have, deferred) `POST /chat/sessions/:id/escalate` is documented in `design/api_contracts.md` but doesn't exist as a route — escalation only happens via the AI tool. Needs either a decision-log entry (like DEC-07) or a doc update.
5. (known, accepted — DEC-10) `EscalationFlag.resolutionNotes` is a single field reused for both the AI's original escalation-context note and the admin's eventual resolution note — resolving an escalation overwrites the original note. Low impact (the original note is visible in the UI before resolving, and now also in the full conversation transcript via UJ-19's new "Ver conversación" link), not planned to be fixed unless it matters in practice.
6. (known, accepted — DEC-11) `AssistantPolicy.value` (UJ-18 policy text) has no prompt-injection content sanitization beyond a length cap — accepted given the single-admin trust model, flagged as a defense-in-depth item to reconsider if admin roles with different privilege levels are ever introduced.
7. ~~(resolved 2026-08-06) Logo integration.~~ Done — favicon, chat header, admin login, and admin sidebar all now use real logo assets derived from `frontend/public/logo-oopss-nails.png`. No open blocker here anymore.
8. **(active, 2026-08-07) Google Sheets sync built but not activated.** `backend/src/lib/googleSheets.ts` is done, tested, and wired into every booking path — it just silently no-ops until the client provides real Google Cloud credentials. Needs: (a) a Google Cloud project + service account with Sheets API access, (b) the target spreadsheet shared (Editor) with the service account's email, (c) the service account's JSON key + the spreadsheet ID pasted into `/admin/credenciales` (`GOOGLE_SERVICE_ACCOUNT_JSON`, `GOOGLE_SHEETS_SPREADSHEET_ID`). No code changes needed once she provides these — the next booking will start appending rows automatically.

## Recent Changes

- 2026-08-03: Planning complete, plan approved, all planning/design/implementation docs populated.
- 2026-08-03: **M0 (Foundation) complete** — IT-01 through IT-10, independently reviewed (PASS, 3 of 4 findings fixed same-session).
- 2026-08-03/04: **M1 built.** Seeded the full real catalog (67 services), 6 specialists with correct assignments/schedules, term synonyms. UJ-03 (`/servicios`) fully verified live. UJ-01/02/04 (chat) built and pipeline-verified without a key, then fully verified once the user provided one — including catching and fixing a real term-equivalence bug (synonyms were seeded at category-level, not service-level; fixed + re-verified).
- 2026-08-04: **M2 (Booking Flow) built and fully verified live**, in the same session, at the user's request to keep going (and to add reschedule capability, DEC-08):
  - `checkAvailability()` (`backend/src/lib/availability.ts`) — the scheduling engine shared by the public endpoint and the `check_availability` AI tool. Verified against real data: Yez's Saturday cutoff and Ana María's 11am start both exact.
  - `createAppointment()`/`rescheduleAppointment()` (`backend/src/lib/booking.ts`) — double-booking prevented by a **DB-level partial unique index**, not just app logic. Booked a real appointment through an actual multi-turn AI conversation (not scripted), confirmed correct in the DB; double-booking attempt correctly got 409; reschedule and cancel both verified live via API and in-browser (`/mis-citas`, new page).
  - Escalation (`escalate_to_human` tool, factory-built per-conversation) verified live: an ambiguous facial question correctly created an `EscalationFlag` assigned to Sonia and flipped the conversation to `handed_off`.
  - **Two real bugs found via live testing, both fixed**: (1) the model invented a fake alternate service name and kept reusing it in tool calls until it silently failed — fixed with a stricter system-prompt rule to always use `get_service_info`'s exact returned name; (2) `POST /appointments` and `.../reschedule` returned a differently-shaped response than `/lookup` and `/cancel` (`specialistName` string vs `specialist: {id,name}` object), which crashed the `/mis-citas` page after a reschedule — fixed by unifying the shape, with regression-guard test assertions added so it can't silently reappear.
  - `prisma migrate dev` confirmed to reliably refuse non-interactive execution in this shell whenever there's a warning — the hand-write-migration-SQL-then-`prisma migrate deploy` workaround is now a settled pattern (used 3 more times this session: `Specialist.name`/`Service.name` unique, `Client.phone` unique, `Appointment` partial unique index).
- 2026-08-04: **{M1+M2} `/review` run and closed.** One critical finding (`POST /appointments` fully public, no auth, no rate limit — anyone could `curl` real bookings directly, bypassing the chat flow entirely) fixed same-session: added `requireSessionOrAdmin` + `selfServiceLookupLimiter`, matching the existing cancel/reschedule pattern; confirmed neither the AI's `create_appointment` tool (calls `createAppointment()` in-process) nor any frontend code was relying on the old public route. Test suite grew from 97 to 100 tests, all passing; build/lint clean. Two lower-priority findings deferred (undocumented escalate route; UJ-05 still unverified).
- 2026-08-04: **M3 (Admin Foundations) built and fully verified live.** New admin routers: `admin/services.ts`, `admin/serviceCategories.ts`, `admin/specialists.ts` (CRUD + `/services` assignment + `/schedule` + `/schedule-exceptions`), all soft-delete (`active=false`) since Service/Specialist are FK-referenced by booking history. New admin frontend pages: `DashboardPage`, `ServicesAdminPage`, `StaffAdminPage`, `SpecialistSchedulePage`. Verified live end-to-end: a price edit propagated instantly to public `/servicios`; deactivating/reactivating a service round-tripped correctly including its `range` price; unassigning a service from Yez immediately removed her from that service's public specialist list and reassigning restored it (UJ-14's core guarantee); adding a day-off exception for Yez dropped her real `/availability/check` slot count from 18 to 0 (UJ-15's core guarantee). One real bug found+fixed: schedule-exception creation 500'd because the date string wasn't converted to a full ISO datetime before hitting Prisma's `@db.Date` column — same pattern `booking.ts` already used correctly, just missed here. Test suite grew from 100 to 138, all passing; build/lint clean on both backend and frontend.
- 2026-08-04: **M4 (Admin Operations & Governance) built and fully verified live — all 19 UJs and 10 ITs now complete.** New admin routers: `admin/appointments.ts` (list+filter, edit reuses `rescheduleAppointment()` for date/time changes so conflict rules always match booking), `admin/knowledgeBase.ts` (synonyms CRUD, policies upsert+delete), `admin/escalations.ts` (list+filter, resolve). New admin frontend pages: `AppointmentsAdminPage`, `CredentialsAdminPage`, `KnowledgeBaseAdminPage`, `EscalationsAdminPage` — the admin sidebar's 7 sections are now all real, no placeholders left (`AdminPlaceholderPage.tsx` deleted). Verified live: wrote the real `OPENAI_API_KEY` into the credential vault (never displayed, health check confirmed it still works, vault now correctly takes priority over `.env`); created/rescheduled/status-changed a real test appointment through the admin UI; added/deleted a test synonym and policy; created/resolved a test escalation. One verification mistake (not a code bug) happened and was self-caught: an imprecise browser DOM query deleted a real seeded synonym instead of the intended test one — noticed immediately, restored via direct API call, final state confirmed to exactly match the pre-test baseline. Test suite grew from 138 to 164, all passing; build/lint clean on both backend and frontend. DEC-10 logged (policies DELETE endpoint added; `EscalationFlag.resolutionNotes` field-reuse tradeoff documented).
- 2026-08-04: **`{M3+M4}` `/review` run and closed (independent subagent, then spot-checked directly by re-running tests/build/lint and re-verifying the fix live in-browser).** One should-fix finding: UJ-19's escalations page had no way to view the chat transcript behind an escalation, despite `design/ui_wireframes.md`/`user_journeys.md` explicitly calling for a "Ver conversación" link. Fixed same-session: `GET /admin/conversations/:id` (new, admin-gated, read-only) + inline transcript toggle in `EscalationsAdminPage.tsx`, 3 new tests. Test suite grew from 164 to 167, all passing; build/lint clean both sides. One nice-to-have logged as an accepted risk rather than fixed: `AssistantPolicy` text has no prompt-injection sanitization beyond a length cap (DEC-11) — low risk since only an already-fully-trusted admin can write it. **All 19 UJs now show `Review Passed: yes` in `task_tracker.md` — no milestone work remains.**
- 2026-08-04: **Demo-data seed pass + full page/empty-state verification, done before the security audit.** Extended `backend/prisma/seed.ts` (idempotent, safe to re-run via `npm run seed`) with 5 demo clients (fake `809555100X` phone block, easily identifiable as non-real), 7 demo appointments spanning all 5 statuses (`pending_confirmation`/`confirmed`/`cancelled`/`completed`/`no_show`) across dates before/on/after "today," 3 demo conversations with messages (one linked to an open escalation assigned to Aurora, one to a resolved escalation assigned to Sonia), and 1 sample `AssistantPolicy` (cancellation policy text). Verified every public page (`/`, `/servicios`, `/mis-citas`) and every admin page (dashboard, servicios, personal, horario, citas, escalamientos, credenciales, conocimiento) live in-browser with this data — no broken layouts, no console errors, dashboard's "citas de hoy"/"escalamientos abiertos" widgets showing real counts. Then cleared all demo data (clients/appointments/conversations/messages/escalations/policy — real catalog/staff/schedule untouched) and re-verified every page's empty state renders a graceful Spanish message instead of crashing or showing a blank screen — confirmed for dashboard, citas, escalamientos, conocimiento, and `/mis-citas`' lookup-not-found path. Restored the demo data afterward via `npm run seed` (`main()` upserts, so it landed identically). Also cleared one stale `ErrorLog` row left over from an earlier bug already fixed this session — an unresolved-looking error in "Errores recientes" would have looked bad in a fresh demo.
- 2026-08-04: **Final holistic pre-delivery security audit run and closed.** Whole-app cross-cutting pass (not per-feature) — every route file, secrets handling, injection surfaces, API exposure, file upload hardening, deployment config, `npm audit`, JWT/session hardening, and the credential vault all re-verified directly (plus a subagent spot-check of the remaining admin routers/AI tools). 2 should-fix findings, both fixed same-session: missing rate limiting on `POST /chat/sessions`, `POST /chat/sessions/:id/messages` (billed OpenAI calls), and `POST /uploads` (unbounded cost/DoS exposure); missing attempt cap on `POST /auth/login` (brute-force exposure on the one admin account). No critical findings. Test suite unchanged at 167/167 (new middleware, no new test-covered behavior); build/lint clean both sides. Full Residual Risks list appended to the end of `docs/work_log.md` for delivery reference.
- 2026-08-04: **Post-delivery iteration #1: brand palette + visual appointments calendar.** User's first change request after delivery. Extracted the real brand color palette from `Nuestros servicios Oopss Nails.pdf` by rendering pages with PyMuPDF and sampling dominant pixel colors (not guessed) — replaced the placeholder pink/mauve theme in `frontend/src/index.css` entirely (light + dark, dark kept per user's explicit choice, retonalized to match). Rebuilt `/admin/citas` with a Google-Calendar-style week view and a specialist-columns day view (both color-coded per specialist via a stable id-hash), switchable alongside the original list. Backend: `GET /admin/appointments` gained `dateFrom`/`dateTo` range support. 169/169 tests passing, build/lint clean (zero warnings). DEC-12 logged.
- 2026-08-04: **Post-delivery iteration #2: click-driven guided booking assistant.** User wanted booking to not require typing — built `GuidedBooking.tsx`, a 7-step modal wizard (categoría→servicio→especialista→fecha→hora→datos→confirmar) that lives alongside the free-text chat, reachable via a persistent "📅 Agendar cita" button on `/`. Deliberately bypasses the AI/LLM entirely for the guided path, using only the same already-public/already-hardened endpoints the chat tools and admin already use — zero hallucination risk, zero new backend surface. Verified live twice (multi-specialist + single-specialist auto-skip paths), both test bookings cleaned up. No backend changes; 169/169 tests still passing.
- 2026-08-04: **Post-delivery iteration #3: chat welcome screen with multi-category checklist.** Refined iteration #2 per user feedback — before typing anything, a new visitor now sees a greeting + checkbox category menu (up to 3) right in the chat, not just a button. Refactored shared booking-step logic out of `GuidedBooking.tsx` into `useBookingFlow.ts` + `BookingStepsPanel.tsx` first (re-verified the modal still worked), then built `ChatWelcome.tsx` on top of the same shared pieces. Verified live: 3-category cap, combined grouped services, real conflict-aware availability, full booking+cleanup, dismiss-to-free-text path. No backend changes; 169/169 tests still passing.
- 2026-08-05: **Post-delivery iteration #4 started: brand polish of the assistant.** User feedback: the chat "no se siente de la marca" — both visual identity and tone/personality need work, confirmed equally important via clarifying questions. User is sending the real logo (pasted inline in chat — not accessible as a file yet, asked them to save it to `frontend/public/`). Visual work that depends on the logo is on hold.
- 2026-08-05: **Post-delivery iteration #4, tone piece: branded greeting for the free-text chat.** User reported (with a screenshot) that typing "hola" got a generic LLM greeting, not anything branded. Added a "SALUDO INICIAL — REGLA ESTRICTA" section to `backend/src/lib/ai/systemPrompt.ts`'s `BASE_PROMPT` forcing a close variation of a specific branded welcome as the assistant's first message, with an anti-pattern warning and an instruction not to repeat it afterward; aligned `frontend/src/components/ChatWelcome.tsx`'s static bubble to match. Verified live via curl against a fresh session (first message = exact branded greeting, second message answered correctly without repeating it, real `get_service_info` data). No backend/contract changes; 169/169 tests still passing. See `docs/work_log.md`'s "Post-delivery iteration #4" entry.
- 2026-08-06: **User saved the real logo** to `frontend/public/logo-oopss-nails.png` (1254×1254, white "oopss!" wordmark + heart-in-the-oo + "NAILS", solid taupe background). Started favicon integration: no ImageMagick/PIL in this environment, but `ffmpeg` is available — used it (with a grid overlay to calibrate coordinates, then iterative crop tests, each checked visually via `Read`) to find a tight square crop of just the "oo + heart" monogram as a favicon candidate. Interrupted by the user's live bug reports (iteration #5, below) before applying it — finished later that session (see iteration #4 visual-piece entry further down).
- 2026-08-06: **Post-delivery iteration #5: three live bugs the user found while reviewing the app.** (1) `/mis-citas` phone field: added live "6030-7210"-style formatting (`formatPhoneInput()`); since the backend previously matched phones by exact string equality, this required a real fix, not just UI — added `backend/src/lib/phone.ts`'s `normalizePhone()` (strips non-digits), applied at both client upsert (`booking.ts`, shared by chat/guided-booking/admin) and self-service lookup/cancel/reschedule comparisons (`routes/appointments.ts`), so formatting is cosmetic-only and never breaks matching (also closes a latent risk of a returning client's differently-formatted phone `upsert`ing a duplicate `Client` row). (2) Reported dashboard load failure wasn't reproducible on a fresh reload — no code defect found, likely a transient dev-server-startup race; flagged to ask the user for exact timing if it recurs. (3) **The real bug behind "el chat público no funcionó"**: traced via `ErrorLog` + the user's own real conversation to a genuine backend defect — `runToolLoop()` (`backend/src/lib/ai/toolLoop.ts`) threw after 4 rounds when the model kept calling tools without settling on a final answer (triggered by "Esta disponible Yez" with no service context), and the user's message got zero reply. Fixed at the root: the loop's last round now always runs with no tools offered at all, forcing a text-only reply — the old hard `throw` is now an unreachable defensive fallback. Backend suite 169→172 (new tests for both the forced-final-round behavior and phone normalization), all passing; build/lint clean both sides. Verified live: dash-formatted phone found a real seeded appointment; a real OpenAI price lookup still worked correctly post-fix (no regression to normal tool-calling); dashboard reloaded clean. Cleared a stale `ErrorLog` row from the now-fixed bug. See `docs/work_log.md`'s "Post-delivery iteration #5" entry.
- 2026-08-06: **Post-delivery iteration #4, visual piece: logo integration.** With the real logo file confirmed saved (`frontend/public/logo-oopss-nails.png`), used `ffmpeg` (no ImageMagick/PIL available) to derive `favicon.png` (256×256 monogram crop) and `logo-lockup.png` (720×293 wordmark crop). Wired the favicon into `index.html` (deleting the old unrelated template placeholder) and the lockup into the chat header, admin login page, and admin sidebar (all previously plain text). Verified live (images load correctly, renders fine in dark theme since the logo has its own opaque background). This closes both halves of iteration #4's "no lo siento de la marca" feedback. No backend changes.
- 2026-08-06: **Post-delivery iteration #6: single-tap selection everywhere, including inside the free-text AI chat.** User shared a WhatsApp-bot screenshot (tap-to-answer buttons attached to each message) and, after clarifying scope via two rounds of questions, confirmed the key requirement: the same click-only categoría→servicio→especialista→fecha→hora flow already built (`GuidedBooking.tsx`'s modal) should be single-tap everywhere it's used, AND should appear *inline mid-conversation* whenever the AI needs the client to pick a service — not just at the welcome screen. Proven necessary by the user's own live test: typing "quiero una cita" got a plain-text question with zero buttons. Extracted the flow into shared `frontend/src/components/booking/ServiceBookingFlow.tsx` (used by the modal, the rewritten checkbox-free `ChatWelcome.tsx`, and now `ChatPage.tsx` inline); added a new AI tool `offer_service_selection` (signal-only, no side effect) that the model calls to trigger the inline picker, with `toolCallMeta` threaded from DB through the SSE stream and `GET messages` to the frontend so it knows when to render it. Caught a real prompt-following bug live (model produced the suggested text without actually calling the tool) and fixed it by making the instruction explicit that text alone doesn't satisfy the rule. Backend suite 172→174; verified live end-to-end with a real OpenAI key — full booking completed via the inline picker (confirmation code `TYS4HQBA`), welcome screen and modal both smoke-tested post-refactor. See `docs/work_log.md`'s "Post-delivery iteration #6" entry.
- 2026-08-06: **Post-delivery iteration #8: deterministic backend safety net for the inline picker.** Client reported "ok" (after the greeting) got a plain-text question instead of the picker — reproduced live and confirmed the same input sequence run twice against the real model called `offer_service_selection` once but not the other time. Client asked explicitly for it to always show right after the greeting. Added `withServiceSelectionFallback()`: forces the picker unconditionally on every conversation's first assistant reply (checked via whether prior history has an assistant message), independent of the model's own tool-calling choice; a text-pattern fallback covers later replies too. Backend suite 174→181. Verified live: 3 fresh "hola" conversations, 3/3 showed the picker via the forced fallback. See `docs/work_log.md`'s "Post-delivery iteration #8" entry.
- 2026-08-06: **Post-delivery iteration #7: specialist name-variant resolution + a real date-hallucination bug found while verifying it.** User reminded that "Yez" is called different things by clients ("Yetzebell", "Yetze") because of her complex real name, and asked that bookings still coordinate correctly with her regardless. Added a "NOMBRES DE ESPECIALISTAS" rule to `systemPrompt.ts`, naming the Yez example explicitly, instructing the model to always resolve to the specialist's exact DB name (from `get_service_info`) before any tool call — mirrors the existing "NOMBRES DE SERVICIOS" rule for services. **While live-testing this, found a real, unrelated, pre-existing bug**: the model had no idea what today's actual date is, so when asked to check availability with a relative date term ("mañana") or no date at all, it guessed — once landing on a date in the past. Fixed in the same pass: `buildSystemPrompt()` now injects the real current date fresh on every call (`FECHA DE HOY: ...`), plus a rule requiring the model to ask for a date rather than invent one. Verified live with a real OpenAI key across two separate conversations: name variants correctly resolved to "Yez" in every tool call, relative dates ("mañana", "el viernes") correctly resolved to the real calendar dates (2026-08-07). No backend/contract changes; 174/174 tests unaffected. See `docs/work_log.md`'s "Post-delivery iteration #7" entry.

## Key Context for Next Session

- Stack: Node.js+Express+TypeScript backend, React+TypeScript+Vite+Tailwind frontend, PostgreSQL 17 (locally installed as a Windows service) + Prisma, JWT+bcrypt admin auth, OpenAI SDK with tool-calling.
- **Local dev database**: PostgreSQL 17 running as Windows service `postgresql-x64-17`. Superuser `postgres`/`postgres`. App role/db: `oopss` / `oopss_dev_pw` / `oopss_nails`. Real values in `backend/.env` (gitignored) — regenerate from `backend/.env.example` if missing. `npm run seed` populates real reference data (catalog/staff/schedules/term synonyms/admin user) **plus intentional demo data** (5 fake clients on the `809555100X` phone block, 7 demo appointments across all statuses, 3 demo conversations/escalations, 1 sample policy) — both idempotent, safe to re-run. Ad-hoc one-off test rows created during live verification sessions (outside the seed script) are still deleted after each check as before; the seeded demo data itself is meant to stay for delivery.
- `.claude/launch.json` has `frontend` (5173) and `backend` (3000) dev configs for `preview_start`. **Restart the backend after any `backend/.env` or `schema.prisma` change** — `tsx watch` doesn't reload env vars, and a running process can lock the Prisma query engine DLL on Windows (kill it before `prisma generate`).
- Seeded admin (dev only): `admin@oopssnails.local` / `DevOnly_ChangeMe123`. **`OPENAI_API_KEY` is now configured in BOTH `backend/.env` AND the Level-3 vault** (`/admin/credenciales` — written live during M4 verification with the real key, vault takes priority per `apiKey.ts`'s vault-first lookup).
- **House rules established**: (1) all user-facing strings must be Spanish, verified by looking at rendered output, not just code; (2) `prisma migrate dev` doesn't work non-interactively here — hand-write migration SQL (correct UTC timestamp via `date -u +%Y%m%d%H%M%S`) + `prisma migrate deploy` instead; (3) `git status` before assuming anything is "committed" — a repo now exists as of 2026-08-07 (iteration #16), local-only `user.name`/`user.email` set on it (not `--global`); (4) talk to the user in Spanish in chat, code/comments stay in English; (5) **for any endpoint returning the same kind of resource from multiple routes (e.g. an Appointment), make sure the response shape is identical everywhere** — a mismatch only surfaces at runtime in the frontend, not at compile time, and cost real debugging time this session; (6) **for AI-behavior-dependent journeys, always test actual live model output**, not just the plumbing — two real bugs this session were prompt/behavior issues invisible to mocked tests and code review; (7) **when multiple rows in a list share an identical action-button label (e.g. several "Eliminar" buttons), scope any DOM query used for live verification to that specific row's own container** — a loose document-wide `querySelectorAll('div').find(...)` grabbed the wrong row once this session and deleted real seed data (caught and fixed immediately, but avoidable).
- Browser-tool note: clicking by a cached `ref` from an earlier `read_page` can silently fail to trigger React handlers in this session (form submits/button clicks did nothing, no error). Workaround used: `element.click()` / `form.requestSubmit()` via `javascript_tool`, or re-`read_page` immediately before clicking.
- **PDF-to-image tooling**: this environment has no `pdftoppm`/poppler, so the `Read` tool can't render PDF pages to view them. Workaround that worked: `python -m pip install pymupdf` into the venv already on `PATH` (`C:\Users\rbrav\AppData\Local\hermes\hermes-agent\venv\Scripts\python.exe`), then use `fitz` (PyMuPDF) directly — `page.get_pixmap(dpi=100).save(path)` — to render pages to PNG, saved into the session's scratchpad directory (NOT `/tmp`, which Bash sees but the `Read` tool can't resolve back to a Windows path). Used this to extract exact brand colors via pixel sampling for DEC-12.
- Read `design/design_summary.md` first every session. Check `implementation/task_tracker.md` for authoritative per-item status.
- Milestone order: M0 done+reviewed → M1+M2 done+reviewed → M3 done → M4 done → {M3+M4} done+reviewed → demo data + page/empty-state verification done → final security audit done → **delivered** → post-delivery #1 done (brand palette + calendar) → #2 done (guided booking assistant) → #3 done (chat welcome + multi-category checklist) → #4 done (agent brand polish — branded greeting + logo/favicon/header/login/sidebar, both halves closed) → #5 done (phone format + tool-loop hang fix, both from live user bug reports) → #6 done (single-tap selection everywhere, incl. inline in the AI chat via the new `offer_service_selection` tool) → #7 done (specialist name-variant resolution + date-hallucination fix) → #8 done (deterministic backend safety net forcing the inline picker on every conversation's first reply) → #9 done (small copy polish on the picker steps) → #10 done (fixed over-eager escalation for services that don't need one) → #11 done (SSE auto-reconnect bug fixes; identified and documented a separate Vite-dev-proxy-only limitation, not fixed since it doesn't affect production) → #12 done (booking steps stay visible as a running history instead of replacing themselves) → #13 done, not yet activated (Google Sheets live sync + email field + input formatting — code complete, waiting on the client's real Google Cloud credentials) → #14 done (real root cause of the chat "necesito refrescar" bug — dropped SSE reply) → #15 done code-wise but the client's actual symptom is NOT confirmed resolved (see Blockers #9) → #16 in progress (`git init` done; Docker build + EasyPanel deploy pending a GitHub repo URL and EasyPanel details from the client).
- Open assumptions (see `planning/questions.md`): currency=USD, Acrylic extension = Tania/Mariangely, no specialist logins in v1, estimated durations, no client password accounts.
- `/review` ran after M0, after {M1+M2}, and after {M3+M4} — all closed. The mandatory pre-delivery holistic security audit (a separate checklist item from milestone `/review`) also ran and closed 2026-08-04 — see `docs/work_log.md`'s "Final holistic pre-delivery security audit" and its "Residual Risks" section.
